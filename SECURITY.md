# Security Note

The original project material contained Wi-Fi credentials and a Telegram bot token.
Those values are intentionally excluded/replaced in this GitHub-ready package.

Before publishing any repository publicly:
- Never commit Wi-Fi passwords.
- Never commit Telegram bot tokens, API keys, or private credentials.
- If a real token was previously exposed, revoke/regenerate it.
